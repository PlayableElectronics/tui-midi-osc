# Index architecture

The first slice is deliberately two-process and sequencing-only. Rust owns project files, the Ratatui UI, supervision, OSC transport, and the native MIDI boundary. A headless `sclang` owns staged/live managed pattern environments, a real `Pbind` event stream, its clock, and normalized event production. The stream is consumed as Events rather than played through the default Event prototype, so no synth or audio server is started. Quitting the UI stops the supervised child so the prototype cannot leave an unintended `sclang` behind.

The OSC protocol is versioned under `/index/v1`. Rust retries `/hello` until `/ready(version, sc_monotonic_seconds)`, then retries `/project/sync(request_id, tempo, ...)` until the matching `/project/sync_ack(request_id, staged_revision, live_revision, name)`. Stage and commit acknowledgements are accepted only when their request ID is outstanding. `/pattern/set(request_id, revision, ...)` must advance the SC staged revision; `/pattern/commit(request_id, revision, mode)` accepts only `now` or `bar` and only the exact staged revision. Replies are `/pattern/staged(request_id, staged_revision, live_revision)` and `/pattern/committed(request_id, staged_revision, live_revision, mode, beat)`. Errors are `/error(request_id, category, message)` where category is `evaluation`, `validation`, `protocol`, `transport`, `midi`, or `engine_fatal`; recoverable errors do not stop playback. Pattern arrays remain validated CSV strings in v1 because the installed SuperCollider OSC API has less predictable nested-array coercion across versions. Event timestamps are monotonic seconds from the SC process start; Rust maps that domain at handshake and uses a deadline-ordered cancellable output scheduler, without claiming sample-accurate native MIDI scheduling.

The single persisted output configuration is `project.toml` metadata: `device.backend` is `monitor` or `midi`, `device.logical_name` is the user-facing destination, and `device.system_port` is the exact native port name. A missing saved port remains disconnected and is never remapped by list position. The current slice has one selected output; per-event multi-destination routing is future work.

Future work, not implemented in this slice: reusable sequencing tools/agents; tracker and indexed views over managed SC objects; arrangements and scenes; modulation matrices for event/control messages; device profiles; native SC livecoding UX; and NerdSEQ, Dyaxis, and custom OSC-node integration. Clock sources should later implement `Internal`, `MIDI input`, `NerdSEQ`, and `OSC` behind a common interface.
